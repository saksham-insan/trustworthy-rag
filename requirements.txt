# --- Core ---
python-dotenv==1.0.1

# --- Document extraction ---
pymupdf==1.24.10          # fast, handles most PDFs well (fitz)
pdfplumber==0.11.4         # fallback for tricky tables/layouts

# --- Embeddings (CPU-only) ---
sentence-transformers==3.0.1
torch==2.3.1               # CPU wheel; installed separately, see README

# --- Vector DB ---
chromadb==0.5.5

# --- LLM APIs (free tiers) ---
google-generativeai==0.7.2   # Gemini
groq==0.9.0                  # Groq (Llama/Mixtral, very fast free tier)

# --- Backend / Frontend ---
fastapi==0.112.0
uvicorn==0.30.5
streamlit==1.37.1

# --- Eval / storage / utils ---
pandas==2.2.2
tqdm==4.66.5
pydantic==2.8.2
