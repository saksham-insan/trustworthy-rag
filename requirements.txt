# --- Core ---
python-dotenv

# --- Document extraction ---
pymupdf
pdfplumber

# --- Embeddings (CPU-only) ---
sentence-transformers
# torch installed separately (already done) — do not re-pin here

# --- Vector DB ---
chromadb

# --- LLM APIs (free tiers) ---
google-generativeai
groq

# --- Backend / Frontend ---
fastapi
uvicorn
streamlit

# --- Eval / storage / utils ---
pandas
tqdm
pydantic